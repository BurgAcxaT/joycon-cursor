name: joycon_cursor
description: Joy-Con Gyro Cursor Driver for Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.0
  permission_handler: ^11.0.0

flutter:
  uses-material-design: true
